import { StyleSheet } from 'react-native';

const signupStyles = StyleSheet.create({
  screen: {
    flex: 1,
    backgroundColor: '#fff',
  },
  scrollContent: {
    paddingBottom: 50,
  },
  field: {
    marginBottom: 24,
  },
  inputText: {
    color: '#111111',
  },

});

export default signupStyles;
